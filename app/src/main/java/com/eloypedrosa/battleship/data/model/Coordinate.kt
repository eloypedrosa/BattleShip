package com.eloypedrosa.battleship.data.model

// Usa valores por defecto para generar el constructor sin argumentos requerido por Firebase
data class Coordinate(
    val x: Int = 0,
    val y: Int = 0
)